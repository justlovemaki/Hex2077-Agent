services:
  hex2077:
    image: hex2077-agent
    build: .
    container_name: hex2077-agent
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: always
